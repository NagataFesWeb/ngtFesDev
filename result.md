# シニアフルスタックエンジニアによるコードベース & Supabase 包括レビュー結果 (result.md)

本ドキュメントは、長田高校文化祭 Web アプリケーション (`NgtFes26` / `ngtFesDev`) のソースコードおよび接続されている Supabase プロジェクトをシニアフルスタックエンジニアの視点から調査・評価した結果をまとめたものです。

---

## 1. プロジェクト概要

### このアプリは何を作ろうとしているのか
神戸市立長田高校の文化祭において、**来場者の利便性向上**および**クラス・全体の運営効率化**を実現するための統合 Web アプリケーションです。紙の整理券や混雑による混乱を防ぎ、デジタル上でリアルタイムに状況を把握・操作できる環境を提供します。

### 想定されるユーザー
1. **一般来場者・生徒 (Visitor)**: 主にスマートフォンからアクセス。企画の待ち時間・混雑度の確認、ファストパス（整理券）の発行・保持・キャンセル、長田検定（クイズ）の受検、お知らせの閲覧。
2. **各クラス・パビリオン運営者 (Operator)**: 各自のスマートフォンやPCからアクセス。自企画の混雑度の3段階更新、来場者の整理券QRコードのスキャン・消し込み、自企画の画像・説明文編集。
3. **文化祭実行委員会・システム管理者 (Admin)**: PCブラウザからアクセス。全企画の混雑度一括制御、ファストパス時間枠・定員の動的変更、システム全体の機能制限（Feature Toggles）、お知らせ投稿、データ初期化。

### 主な機能
- **リアルタイム混雑度・待ち時間表示**: LVL 1 (空き) 〜 LVL 3 (混雑) のリアルタイム変更および推定待ち分数の表示。
- **ファストパス (整理券) システム**: 時間枠ごとの定員制御、発券、マイページQR表示、カメラQR消し込み、キャンセル機能。
- **長田検定 (クイズ) & 称号・壁紙報酬**: ランダム10問出題、クライアント採点・署名検証、累計スコアランキング、到達称号付き限定壁紙の signed URL 配布。
- **運営者・管理者ダッシュボード**: ロール別の機能制限および操作ログ記録。

### 現在の完成度 (推定)
**88 / 100 点 (完成度: 約 90%)**
主要機能およびバックエンド RPC（Database Functions）によるデータ整合性・排他制御はほぼ完成状態にあります。本番運用に向けた残課題は、デバッグ用隠しページの削除、高負荷時接続プール (Transaction Pooler) の最終確認、およびテストケースの拡充です。

---

## 2. Supabase の構成

### テーブル一覧 & 各テーブルの役割
| テーブル名 | 役割 | レコード数 (現状) | RLS |
| :--- | :--- | :--- | :--- |
| `users` | 来場者アカウント管理 (`auth.users` と同アトミック連携) | 756 | 有効 |
| `classes` | 運営クラス・団体のログイン認証マスター | 46 | 有効 (Policyなし = RPC専用) |
| `projects` | 全企画のマスターデータ (場所, スケジュール, 回転率等) | 45 | 有効 |
| `congestion` | 各企画のリアルタイム混雑度 (1:1 リレーション) | 41 | 有効 |
| `fastpass_slots` | 整理券の時間枠・定員・区分 (`school`/`public`) | 22 | 有効 |
| `fastpass_tickets` | 発券された整理券の実体 (QRトークン, 使用フラグ) | 185 | 有効 |
| `quiz_questions` | 長田検定の出題問題・選択肢・解説データ | 68 | 有効 |
| `quiz_rewards` | クイズの称号到達スコアと壁紙ファイルパス | 6 | 有効 |
| `quiz_scores` | ユーザーごとの最高スコア・累計スコア・プレイ回数 | 270 | 有効 |
| `news` | トップページに表示するお知らせデータ | 5 | 有効 |
| `operation_logs` | 運営者・管理者の操作ログ・監査ログ | 418 | 有効 |
| `system_settings` | 機能トグル (整理券受発券ON/OFF等) の集中設定 | 7 | 有効 |

### テーブル同士のリレーション
- `classes` (1) ── (N) `projects`
- `projects` (1) ── (1) `congestion`
- `projects` (1) ── (N) `fastpass_slots`
- `fastpass_slots` (1) ── (N) `fastpass_tickets`
- `users` (1) ── (N) `fastpass_tickets`
- `users` (1) ── (1) `quiz_scores`

### Storage Buckets
1. **`public-assets`** (Public: ON): 会場マップ (`venue-map-booth.webp`)、タイムテーブル (`timetable-stage.webp`)。
2. **`quiz-rewards`** (Public: OFF): クイズ報酬の限定壁紙画像。RPC `get_quiz_reward_url` を通じて署名付き URL (Signed URL) で安全に配布。
3. **`project-images`** (Public: ON): 運営者がダッシュボードからアップロードする各企画の展示画像。

### Edge Functions
- **0件 (`functions: []`)**: 全てのビジネスロジックは Supabase 内の PostgreSQL Database Functions (RPC) に完全に集約されています。メンテナンス性が高く、レイテンシ面でも非常に有利な構成です。

### Database Extensions
- 有効化済み: `pg_stat_statements` (クエリ分析), `uuid-ossp` (UUID生成), `supabase_vault`, `plpgsql`, `pgcrypto` (暗号化処理)。

### RLS (Row Level Security) の設定状況
- 全 12 テーブルで RLS が有効化されています。
- 公開参照用テーブル (`projects`, `congestion`, `fastpass_slots`, `news`, `quiz_questions`, `system_settings`) はパブリック `SELECT` が許可。
- 個人所有データ (`users`, `fastpass_tickets`, `quiz_scores`) は `user_id = auth.uid()` の本人のみ `SELECT` / 一部 `UPDATE` が許可。
- **データ更新の堅牢性**: 直接の `INSERT` / `UPDATE` は原則禁止されており、ビジネスロジックはすべて `SECURITY DEFINER` 付きの RPC 関数経由で実行されるため、不正なデータ改ざんが困難な高度なセキュリティ構成となっています。

---

## 3. コードベースとの整合性

### 使用されているテーブル
- `users`, `projects`, `congestion`, `fastpass_slots`, `fastpass_tickets`, `quiz_questions`, `quiz_rewards`, `quiz_scores`, `news`, `system_settings`
- 上記テーブルはフロントエンドまたは Route Handlers から PostgREST クエリ経由で直接参照されています。

### 使用されていない / 直接アクセスされていないテーブル
- `classes`: フロントエンドから `supabase.from('classes')` による直接参照はありません。運営者ログイン RPC (`operator_login`) の内部でのみ使用されており、RLS Policy を設けないことで直接アクセスを遮断する**極めて安全な設計**となっています。
- `operation_logs`: DB 内の各種操作 RPC (`admin_update_congestion`, `verify_and_use_ticket` 等) 内で自動的に `INSERT` されていますが、フロントエンドの管理者画面でログを一覧表示する UI が未実装です。

### 不整合および注意点の指摘
1. **隠し / デバッグページの残留**:
   - `app/quiz/system-override-992-delta-v7-private-2026/page.tsx` というデバッグ用の隠しルートが存在します。クイズの特定の報酬確認用と見られますが、本番環境に露出するとセキュリティ・運用の懸念となります。
2. **RPC 関数の呼び出し重複**:
   - クイズ採点処理において、API Route 内で `submit_quiz_score_batch` が使われていますが、単体の `submit_quiz_score` も DB 上に存在します。

---

## 4. アーキテクチャレビュー

### 優先度別 重大問題・懸念点

#### 🔥 High (高): 本番運用前に修正・対応必須
1. **デバッグ用隠しページの露出リスク**:
   - `app/quiz/system-override-992-delta-v7-private-2026/page.tsx` がそのまま残存しています。本番デプロイ前に削除するか、管理者認証ガードをかける必要があります。
2. **高負荷時 (3,000人規模) の DB コネクション枯渇リスク**:
   - 今後のピークアクセス時、Supabase の PostgreSQL 直接接続 (Port 5432) では接続上限に達する恐れがあります。接続プール (Transaction Mode Pooler: Port 6543) または Supabase Data API (HTTP) の利用を確実に徹底する必要があります。

#### 🟡 Medium (中): 開発効率・運用性のために対応推奨
1. **`operation_logs` の管理者画面未表示 (監査機能の未完成)**:
   - DB側で 400 件以上の操作ログが正しく蓄積されているものの、管理者ダッシュボードで閲覧する UI が存在しません。不正操作の早期発見のためにログ閲覧画面の追加が望まれます。
2. **Vitest 単体テストカバレッジの不足**:
   - `src/lib/fastpass.test.ts` などの一部テストは存在しますが、重要ロジックである RPC 呼び出しや Hook 群のテストが不足しています。

#### 🟢 Low (低): コードの美しさ・クリーンアップ
1. **LINE ログイン・旧投票機能のコード跡地整理**:
   - データベースマイグレーションでは既に削除・移行済みですが、ドキュメントや古いコメントに名残があるため表現を完全統一することを推奨します。

---

## 5. 改善提案 (最初の1週間でやること)

もし自分がこのプロジェクトを引き継いだ場合、最初の1週間で以下の優先順位で作業を行います。

### 1. すぐ直すべき問題 (Day 1 - 2)
- [ ] **隠しデバッグページの削除・アクセス制限**: `app/quiz/system-override-992-delta-v7-private-2026` の削除。
- [ ] **環境変数と Supabase Connection Pooler の動作検証**: 高負荷対策としての Port 6543 経由接続テストの実施。

### 2. 技術的負債の解消 (Day 3 - 4)
- [ ] **操作ログ (`operation_logs`) 閲覧画面の管理者ダッシュボードへの追加**: 蓄積されたログを活用可能にする。
- [ ] **RPC 関数の重複整理**: `submit_quiz_score` と `submit_quiz_score_batch` の統一。

### 3. 将来的な改善 & おすすめの追加機能 (Day 5 - 7)
- [ ] **運営者セッションの自動ログアウト/有効期限設定**: 共有端末での離脱対策。
- [ ] **主要 RPC / Hooks に対する Vitest 単体テストの拡充**: 回帰バグ防止の自動化。

---

## 6. 最終まとめ

### このプロジェクトの強み
1. **優れたバックエンドカプセル化 (RPC 優先設計)**: 発券・スキャン照合・混雑度変更等の重要処理が DB 内の RPC に閉じており、レースコンディションや改ざんに極めて強い。
2. **モダンで整備されたフロントエンド構成**: Next.js 16 (App Router), Tailwind CSS 4, Shadcn UI, TanStack Query が洗練された形で統合されている。
3. **明確な権限分離 (RLS & 3層認証)**: 来場者・運営者・管理者の認可モデルが強固に分離されている。

### 現在の問題点トップ5
1. デバッグ用隠しルーティング (`system-override...`) の残留。
2. 3,000人規模のピークアクセスに対する接続プール (Port 6543) の最終本番テスト未実施。
3. DBに蓄積されている `operation_logs` (操作ログ) の管理者 UI 未実装。
4. クイズ送信用 RPC (`submit_quiz_score` / `submit_quiz_score_batch`) の重複存在。
5. 単体テスト (Vitest) のカバレッジ不足。

### 次に実装すべきことトップ5
1. デバッグ用ルーティングの削除およびセキュリティチェック。
2. 管理者ダッシュボードへの「操作ログ閲覧タブ」の追加。
3. Supabase Transaction Pooler 経由での k6 負荷テストの実施。
4. 運営者ダッシュボードのセッション安全ログアウト機能の強化。
5. Vitest による RPC 連携クライアントロジックのテストコード拡充。

### 総合評価
# **88 / 100 点**
*(非常に高い品質で設計されており、文化祭本番での成功が強く期待できる優れたプロダクトです)*
